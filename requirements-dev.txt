# Development requirements for Firefly Cloud Home Assistant integration
# Requires Python 3.13+

# Home Assistant core
homeassistant>=2024.1.0

# Core dependencies with compatibility fixes
aiohttp>=3.8.0
aiodns==3.1.1  # Pin to 3.1.1 for Python 3.13 compatibility (3.2.0 has type hint issues)
pycares<5.0.0  # Pin to <5.0.0 for Python 3.13 compatibility (5.x has type hint issues with aiodns)
lxml>=4.9.0
lxml-stubs  # Type stubs for lxml
python-dateutil>=2.8.0
voluptuous
async-timeout

# Compatibility fixes for josepy/acme conflict
josepy<1.14.0
acme>=1.21.0

# Development tools
black
flake8
mypy
pylint
isort
yamllint
codespell

# Testing framework
pytest>=6.2.5
pytest-asyncio
pytest-cov
pytest-xdist
pytest-mock

# Additional testing utilities
freezegun  # For date/time mocking
responses  # For HTTP mocking

# Missing Home Assistant dependencies
numpy  # Required by stream component
scipy  # Often required alongside numpy